name: javdb-cover-bot

services:
  bot:
    build: .
    image: javdb-cover-bot:local
    container_name: javdb-cover-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      TMP_DIR: /app/data/tmp
    volumes:
      - ./data:/app/data
